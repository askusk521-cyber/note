---
title: 公开数据优先v0.2：不新增DFT也可以开始模型研究
date: 2026-09-21
type: pilot-specification
status: 更新开工顺序；公开数据读取已核查；未新增量化或运行模型
tags: [AI4Science, 数据集, 实验设计, 研究规划]
---

# 公开数据优先v0.2：不新增DFT也可以开始模型研究

**现成数据足够开始模型复现、训练、结构生成评估及公开反应数据分析；新增DFT不再作为第一阶段的前置条件。** 前一版把目标氨酰化的物理标签生产与模型开工放得过近，现将两者明确分开。本文件更新《开工规格v0.1》的时间安排，并不声称现有数据已覆盖全部目标化学。

## 可直接复用的公开数据

| 数据 | 已有标签及规模 | 不新增DFT可做的事 | 数据边界 |
|---|---|---|---|
| **UniTS-Lib** | 4,391条记录；DFT优化TS坐标、频率/振动模、电子态及图/位点等 | 几何生成与修正、条件编码、手性/多样性和留出泛化 | 建库未逐条做IRC；原基准条件部分来自参考TS；没有完整反应物水合构象—接力—TS配对 |
| **RGD1** | 约12.7万个独立反应、17.7万条含构象差异的TS记录；R/TS/P与能量等 | 双端条件TS生成、相对参考结构的几何指标、已有能垒标签预测 | CHNO、中性闭壳层；xTB有IRC，DFT端点主要由分类器判定，非全部DFT-IRC |
| **Transition1x** | 10,073条收敛反应，约960万条路径附近构型与DFT能量/力 | 反应势学习、路径表示、端点条件几何基准 | CHNO小分子；路径构型不是逐条精修并验证的鞍点 |
| **Kingfisher公开数据库** | 甲醛水合、CO₂水合、水/甲醇环境、苯酚氯化等已计算网络 | 提取已算的溶剂参与路径、复查已有结构与能量；构造小规模环境条件诊断集 | 少数化学家族；需要数据库转格式，并区分量化层次、区域和验证等级 |
| **DORTS-9K** | 9,017个反应、874,176个xTB路径附近构型的DFT能量/力 | 在已有配对构型上训练/评价Δ势 | 标签为DFT单点，不等于每个TS都在DFT重新验证；不直接替代溶剂接力TS标签 |

这些数据的DFT是作者已经完成的工作。下载、解析、划分和用现有标签训练，不需要再运行DFT。模型类型决定应选择哪种标签，而不是所有研究都必须从头生产量化数据。

## 本轮实际读取了什么

1. 对官方ModelScope中的固定版本 `dataset/UniTS_Lib.npy` 做HTTP部分读取，返回206；文件头为NumPy对象数组，**shape为(4391,)**，整体长度1,439,687,943字节。故确认文件可获取，不只是文献声称公开。尚未下载全量并反序列化核对4391条的所有字段。
2. 读取Kingfisher压缩包的ZIP目录与内部README，识别**55个归档条目**。其中含甲醛/水、CO₂/水、CO₂/水甲醇、苯酚/次氯酸/水等MongoDB归档，以及阈值对照数据库、分析脚本和输入。README明确区分已有数据库与启动新计算的脚本；我们只需提取已有数据，不执行那些启动搜索或精修的脚本。
3. 前轮已核查RGD1、Transition1x、DORTS论文的标签定义及正式数据入口。它们的全部记录尚未在本地逐条审计，不能把资料可获得与完整质量审核等同。

证据留档：`work/startup_audit/no_new_dft/units_npy_header.bin`、`kingfisher_file_list.json`、`kingfisher_README.md`。未新做任何电子结构计算。

## 第一阶段的具体选择

**继续暂定UniTS-Lib为几何起步集，官方UniTS-Gen为原模型基线；Kingfisher作为溶剂参与的第二阶段诊断数据。** 不再将“先获得目标氨酰化DFT路径”作为开始模型实验的必要条件。

第一阶段优先完成：

1. 获取并整理UniTS-Lib，保留原始记录、来源、坐标朝向和电子态；复现与checkpoint相匹配的划分。
2. 跑官方基线，评价相对公开参考的几何、多样性、碰撞、手性和条件依赖；根据现有标签做模型训练或微调。
3. 提取Kingfisher已有反应物/TS/路径及环境信息，明确哪些条件可从反应物独立获得，哪些标签需要由已有路径事后抽取。
4. 在数据支持时比较增加环境条件是否改善留出结果。仅有几个反应家族时，明确统计范围，不将同一反应的许多水构象当作许多独立化学反应。

**暂时不训练离散接力头的原因是标签未整理，不是必须新增DFT。** 如果现有路径和原子映射足以提取转移方案，可以离线构造标签；若只有静态TS，则不能猜测其完整质子传递顺序。

## 不新增DFT时，评价哪些结论是成立的

可以直接评价：相对既有参考TS的几何误差、立体化学保持、多参考结构覆盖、已有通道/环境标签恢复、分组留出的泛化，以及已有构型上的能量/力预测误差。参考结构不完整时，“多样性高”不能自动称为发现更多正确通道。

不能直接新增的结论：任意新生成坐标的真实DFT能量/力、其是否一阶鞍点及IRC端点、未公开的新反应通道的精确势垒。**参考TS有一个虚频，不表示与它接近的生成结构自动继承相同验证。** 用预训练势或xTB检查可以作为额外代理指标，但必须保留其层次，不能改称DFT验证。

如果从公开数据学习或解释的是已有计算覆盖的现象，完全可以复用作者已有量化结果完成研究。若未来要声称发现了新的硫酯—核苷机理，需要另有独立的量化或实验支持；是否以及何时补做，取决于届时真正要提出的新科学结论。

## 下载与原始来源

- [UniTS官方代码与下载说明](https://github.com/licheng-xu-echo/UniTS)
- [UniTS官方ModelScope数据及权重](https://modelscope.cn/models/XuLiCheng2025/UniTS-Gen-v1)
- [UniTS-Lib固定版本数据文件](https://modelscope.cn/models/XuLiCheng2025/UniTS-Gen-v1/resolve/08bd230e0826b6075431b7e123ee07a8e56c32be/dataset/UniTS_Lib.npy)
- [RGD1 Figshare](https://doi.org/10.6084/m9.figshare.21066901.v6)；[数据论文](https://doi.org/10.1038/s41597-023-02043-z)
- [Transition1x Figshare](https://doi.org/10.6084/m9.figshare.19614657.v4)；[数据论文](https://doi.org/10.1038/s41597-022-01870-w)
- [Kingfisher Zenodo](https://doi.org/10.5281/zenodo.15297813)；[方法论文](https://doi.org/10.1021/acs.jctc.5c00245)
- [DORTS Zenodo](https://doi.org/10.5281/zenodo.17141108)；[方法论文](https://doi.org/10.1038/s41467-026-72945-0)

关联：[[2026-09-21 开工规格v0.1｜数据与第一轮实验]]、[[2026-09-21 架构设计讨论｜前生物氨酰化的条件生成]]。本次调整的是开工顺序，未改变“AI需要服务可解释、可检验的科学问题”的要求。
