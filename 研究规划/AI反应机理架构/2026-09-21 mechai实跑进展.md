> mechai项目文档镜像。主文件：[mechai/docs/STATUS.md](/home/shen/文档/ChatGPT/mechai/docs/STATUS.md)。文中相对路径均相对于项目主目录；最新实验状态以主文件和n2的run.json为准。

# 当前状态与下一步

更新：2026-09-21。此文件交接实际进度，不把启动成功当作方法有效。

## 已完成

- 本地主代码与Git仓库在mechai，未配置远程仓库；历史文献与先导检查仍保留。
- n2独立环境、固定源码部署及Slurm GPU执行已打通。实际环境见`environments/units-n2-freeze.txt`；有效预处理目录是`data/processed/units-v2/`。
- 官方UniTS-Lib（4,391条）及Formula-OOS权重完整下载、SHA256校验完成。字段审计覆盖原子顺序、几何、图特征、电荷/多重度与反应中心。结果见`reports/E0b/audit.json`。
- 重建152个留出分子式组、403条测试和3,988条trainval；与公开notebook计数及checkpoint文件名一致。没有作者独立训练日志，不能进一步证明实际训练成员。
- 官方源码固定为`a7147021becc4e13887be2c756fefc8b5983f390`。兼容补丁只延后非必要量化工具导入、明确权重载入与严格参数检查，不改神经计算/采样公式。
- Slurm job 1969完成2条样本×1候选的完整1,000步采样；采样36.35秒。完整结果在n2的`runs/20260921-134700_E1_smoke_s42/`，摘要在`reports/E1/smoke/`。
- job 1973完成32条×1候选的独立诊断，完整1,000步，704.69秒，全部返回有限几何。固定映射proper RMSD均值3.949 Å、中位数3.617 Å，3个候选含小于0.5 Å的原子对；只代表当前指标下质量有限。摘要见`reports/E1/pilot32-k1/`。
- 最小条件残差及零初始化包装已实现，CPU性质测试通过；架构见`docs/MODEL_V0.md`。其现有条件来自官方图/反应中心，不增加独立溶剂标签。

- 零残差GPU回归：job 1968成功严格载入模型，但逐位跨调用断言失败。官方baseline自身重放同样有4.77e-7的FP32差异，RNG一致、残差恰零。原始失败保留在`reports/E1/adapter-check-v1.json`。后续job 1971的v2已通过同一次调用的逐字节恒等检查、严格零残差与RNG检查；跨调用4.77e-7差异仍单独报告，见`reports/E1/adapter-check-v2.json`。
- adapter-only训练入口已运行：job 1974完成32步真实GPU短训练（15.18秒）；官方17,145,611个参数与buffer不变，6,208个新增参数更新、前层非零梯度与独立checkpoint恢复通过。见`reports/E2/adapter32steps/`。

## 正在推进

- 首批32条Formula-OOS记录、每条8候选的完整基线由job 1972执行（batch size 8，有限时限2.5小时）；job 1973的32×1独立诊断已完成。k=8的匹配k=1必须取1972每条的第一候选，不能把1973直接当作其嵌套结果。
- 首次job 1970因实测总耗时预计超过原1小时时限，而Slurm拒绝延长运行中时限，已主动停止并保留40个候选；新run从头运行，不拼接旧候选或择优替换。摘要在`reports/E1/pilot32-interrupted/`。
- 32条仅含13个分子式组，选取规则是测试文件前32条，不代表全数据化学分布。

## 结果解释与未完成项

- 完成的32条单候选平均反应中心距离MAE为0.908 Å；固定原子对应RMSD与论文允许原子重排/镜像的定义不同。目前不能声称复现了论文报告的数值性能，也不能将有限坐标当作DFT鞍点。
- 新模型只完成工程性32步训练，未证明几何质量改善、新机理成立或方法新颖性。
- 两条smoke经独立CPU诊断，没有发现单位恢复、数据/坐标对应或RMSD公式错误；原始/处理/保存参考完全对应。官方重排指标因缺pymatgen未运行，见`reports/E1/smoke/diagnostics.md`。
- 手性、对称等价原子、候选多样性/参考覆盖率尚待补充评价；当前反应中心距离统计不能替代手性或IRC。
- 未新增任何DFT/xTB，生成候选没有独立TS优化、虚频和IRC验证。
- UniTS-Lib没有独立反应物微溶剂化/质子接力配对标签；目标氨酰化的环境条件假设仍需Kingfisher等数据诊断，不能把当前残差模块直接称为溶剂机理模型。

## 下一步

1. 保存32×8基线候选、运行元数据、逐样本失败与指标。
2. 已完成零残差GPU回归和真实数据adapter-only短训练；下一阶段扩大训练前固定对照与验证协议。
3. 固定分组验证、候选预算和种子，开展最小架构对照；短训练不用于宣称方法有效。
4. 从已有溶剂数据识别可学习的输入/标签，决定是否推进真正的环境条件与离散通道提案。

CPU原先5项检查仍可执行：`python3 -m unittest discover -s reaction_pilot -p 'test_*.py'`。新增下载、模型和指标测试在`tests/`；模型测试需PyTorch/NumPy。全程不以新增量化数据作为起步前提。

## 交接入口

92项CPU测试（新增87＋原有5）通过；Python编译、两份Slurm脚本语法与差异格式检查通过，见`../reports/validation-20260921.json`。所有job、源码快照、远端run位置及小型报告统一列在`../reports/RUNS.json`。后续先读取job 1972的run.json确认完成/失败，再更新本文件；不要重跑已完成的数据接入和训练就绪检查。
