> 2026-09-22从mechai同步。源码规范以仓库为准：`/home/shen/文档/ChatGPT/mechai/docs/PROJECT_SPEC.md`。本页为可读镜像。

# mechai项目规范 v0.2

更新日期：2026-09-22。本版将项目主目录确定为mechai，取代历史v0.1的代码路径与仓库范围；起步无新增DFT、后期独立DFT验证的约束保持有效。本次补充审阅后的文档分工与阶段入口，不改写既有实验协议。

## 目录与维护入口

| 用途 | 位置 |
|---|---|
| 主代码与本地Git仓库 | `/home/shen/文档/ChatGPT/mechai/` |
| 现有先导实现 | `reaction_pilot/` |
| 当前背景、状态、规范 | `docs/PROJECT_CONTEXT.md`、`docs/STATUS.md`、本文件 |
| 参考资料与研发路线 | `docs/REFERENCE_MAP.md`、`docs/RESEARCH_ROADMAP.md` |
| n2主机配置 | `configs/hosts/n2.json` |
| n2项目根目录 | `/home/lhshen/projects/mechai/`，已创建并部署独立环境与固定源码快照 |
| n2代码/数据/运行输出 | 远端根目录下`releases/<code_id>/`、`data/`、`weights/`、`runs/<run_id>/` |
| 本地小型证据缓存 | `data/evidence/startup_audit/`，不进Git |
| Obsidian研究文档 | `/home/shen/文档/Obsidian Vault/研究规划/AI反应机理架构/` |

主代码只在mechai维护。旧工作区保留历史，n2使用固定版本快照，不进行双向自动覆盖同步。源代码同步不得删除远端数据、环境、权重或旧实验。原先`/home/lhshen/projects/reaction-mechanism-ai/`只是未部署的规划路径，现统一使用mechai名称。

现有E0代码先保留，已按实际功能增加`src/mechai/{data,models,baselines}/`、`scripts/`与`tests/`。当前模型合同见`docs/ARCHITECTURE_V0.md`与`configs/experiments/event_architecture_v0.json`，表示/接口已冻结，完成边界逐项记录。不要为了目录美观提前重写可用代码。生产逻辑保存在Python模块，notebook用于探索/绘图。

## 运行与环境

本机用于编辑、轻量CPU检查；n2通过Slurm执行所有真实GPU操作。初次单卡参数为`--partition=main --gres=gpu:pro6000:1 --nodes=1 --ntasks=1`，短GPU检查使用`--time=00:10:00`，训练作业在配置中显式给出有限时长。保留Slurm分配的设备可见性，不绕过调度。

n2独立环境`envs/units/`已运行官方模型，实际依赖锁见`environments/units-n2-freeze.txt`。Python 3.11、PyTorch 2.8.0+cu128、RDKit 2025.3.5及PyG扩展已验证；不修改共享环境。当前有效数据预处理是`data/processed/units-v2/`，早期RDKit版本不匹配的v1输出只保留审计记录。

官方几何评价使用隔离的`envs/units-eval/`覆盖环境，复用已固定训练依赖并增加上游锁定的pymatgen 2025.6.14；实际有效依赖见`environments/units-eval-n2-freeze.txt`。训练环境未改变。该评价仅CPU，有限Slurm作业，不申请GPU。

机器路径集中在主机配置、命令行或配置解析层；模型/数据逻辑不写死个人路径。`experiment_v0.2.json`中的文件引用相对于该配置文件所在目录；E0默认缓存路径由项目根目录推导，可通过参数覆盖。

## 数据与基线

原始数据只读，处理结果另存。跟踪来源/DOI、下载版本、许可证、校验和、理论层次、预处理版本和记录ID。大数据、权重和环境不入普通Git；小型清单、split与摘要进入版本管理。

核对官方Formula-OOS划分与权重后才称为官方复现。新实验记录分组规则，同反应的构象/路径帧不能随机打散造成泄漏。区分参考TS条件与独立输入，保留缺失字段及验证证据等级。

官方代码固定revision并保留来源/许可证，通过适配层调用；必要上游修改单独记录。不得将首个基线同时改造成多个新模块而失去对照。

## 实验记录

run ID采用`YYYYMMDD-HHMMSS_E阶段_短描述_s种子`，记录Asia/Shanghai时区；新实验新目录，恢复任务记录`resumed_from`。

每个run保存：

- `run.json`：假设、状态、时间、主机、Slurm job ID、随机种子、候选数量和计算预算。
- `config.resolved.json`：展开默认值后的实际配置。
- `code.json`：Git commit、上游revision及必要补丁；有未提交改动时另存包含新增文件的完整源码快照和校验清单。
- `data.json`：数据版本、split哈希、实际样本ID和标签来源。
- `environment.txt`、`command.txt`、`stdout.log`、`stderr.log`：依赖、命令与日志。
- `metrics.json`及逐样本候选/失败原因：不只保存最好结果。

首次调试可单种子；架构比较使用匹配的数据、条件、候选预算，原则上复用至少3个种子，未达到时明确为先导证据。分别报告单候选与best-of-k、样本数与独立反应组数。小型结果回收至本地`reports/`，完整轨迹与checkpoint保留在n2并记录位置和校验和。

## 阶段与完成标准

| 阶段 | 需要的证据 |
|---|---|
| E0（已完成小型检查） | 来源、示例解析和科学标签合同检查；不冒充全量数据审计 |
| E0b | 完整数据可读取、字段/单位/来源核对、官方划分与checkpoint匹配 |
| E1 | n2上完成官方基线推理并保存可复现run；初始32个留出样本、每条件1/8个候选 |
| E2 | 围绕一个架构假设完成匹配对照，报告覆盖范围与失败模式 |
| E3（项目后期） | 固定模型与候选选择后独立DFT验证TS、IRC端点和竞争通道势垒 |

首周判断数据适用性、基线复现和架构可检验性。新机理的物理成立性属于后期验证；最终DFT验证若用于调参，需要另留最终测试。

2026-09-22课题审阅后，后续工作按[研发路线](<2026-09-22 课题研发路线与验收.md>)的R0–R4组织：数据/任务合同、最小图事件对照、事件与几何连接、目标体系迁移、独立物理验证。它们是决策阶段，不能因接口实现或上一阶段完成而自动视为下一阶段通过。原E0–E3实验编号、冻结配置及run不重命名；当前进度以[STATUS](<2026-09-22 mechai当前进展.md>)为准。

近期先完成FlowER状态网络、全量身份/独立留出及元素/电荷/自由基/BE合同。图事件任务、端点几何任务、水相竞争路径任务分别建立输入/标签和验收证据；缺少配对或环境标签时，不把两个公开数据接口直接升级为联合机理模型。源TS的41条排序和8条几何诊断支线不继续调参。

## Git、交接与文档

本仓库范围为整个mechai，默认分支`main`。提交规范及强制AI署名见根目录`AGENTS.md`；每次提交前重新读取实际CLI版本和当前模型配置。远程仓库未创建，不将本地初始化写成已经上传GitHub。

每次任务结束更新`docs/STATUS.md`中的实际证据、待办和限制；规范变更更新本文件及相关配置。Obsidian保留可读镜像，新项目规范以本仓库为主；归档材料不覆盖当前决定。
