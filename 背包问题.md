![[Pasted image 20260418140902.png]]
看作一个n轮决策，放入与否，决策树模型。
定义状态dp[i,c],子问题变成了前i个问题在容量为c的背包里最大价值，
求解dp[n,cap],需要一个尺寸为(n+1)*(cap+1)  的二维dp  表。
![[Pasted image 20260418142859.png]]
确定边界条件
无物品或者背包容量为0的时候最大价值为0，两层循环遍历整个dp表即可。
**方法一**暴力搜索
```
def knapsack_dfs(wgt: list[int], val: list[int], i: int, c: int) -> int:
    """0-1 背包：暴力搜索"""
    # 若已选完所有物品或背包无剩余容量，则返回价值 0
    if i == 0 or c == 0:
        return 0
    # 若超过背包容量，则只能选择不放入背包
    if wgt[i - 1] > c:
        return knapsack_dfs(wgt, val, i - 1, c)
    # 计算不放入和放入物品 i 的最大价值
    no = knapsack_dfs(wgt, val, i - 1, c)
    yes = knapsack_dfs(wgt, val, i - 1, c - wgt[i - 1]) + val[i - 1]
    # 返回两种方案中价值更大的那一个
    return max(no, yes)
```
![[Pasted image 20260418160535.png]]
![[Pasted image 20260418160815.png]]
![[Pasted image 20260418161215.png]]
动态规划示意图
还可以进行空间优化，用一个单行数组不断遍历。注意要倒序循环
在空间优化中，由于每个状态依赖正上方和左上方的状态，因此需要倒序遍历列表，避免左上方状态被覆盖。
