唯一策略就是刷题了，带上处理输入输出一起问AI，先看看多久抄完一遍


```
n=int(input())
nums=list(map(int,input().split()))
target=int(input())
dict={}
for i in range(n):
	need=target-nums[i]
	if need in dic:
		print(dic[need],i)
		break
	dic[nums[i]]=i

```



```
#6
#eat tea tan ate nat bat
n=int(input())
words=input().split()
dic={}
for word in words:
	key=''.join(sorted(word))
	if key not in dic:
		dic[key]=[]
	dic[key].append(word)
for group in dic.values():
	print(''.join(group))
```

```
n=int(input())
nums=list(map(int,input().split()))
num_set=set(nums)
res=0
for num in num_set:
	if num-1 not in num_set:
		cur=num
		length=1
		while cur+1 in num_set:
			cur+=1
			length+=1
		res=max(res,length)
print(res)
```

```
n=int(input())
nums=list(map(int,input().split()))
left=0
for right in range(n):
	if nums[right]!=0:
		nums[left],nums[right]=nums[right],nums[left]
		left+=1
print(*nums)
```
nums = [1, 3, 12, 0, 0]

print(nums)
print(*nums)
[1, 3, 12, 0, 0]
1 3 12 0 0
```
n=int(input())
height=list(map(int,,input().split()))
left=0
right=n-1
res=0
while left<right:
	width=right-left
	h=min(height[left],height[right])
	area=width*h
	res=max(res,area)
	if height[left]<heigt[right]:
		left+=1
	else:
		right+=1
print(res)
```

```
n = int(input())  
nums = list(map(int, input().split()))
nums.sort()
res=[]
for i in range(n):
	if i>0 and nums[i]==nums[i-1]:
		continue
	left=i+1
	right=n-1
	while left<right:
		s=nums[i]+nums[left]+nums[right]
		if s==0:
			res.append([  ,  ,  ])
			while left<right and nums[left]==nums[left+1]
```

```
s=input().strip()
dic={}
left=0
res=0
for right in range(len(s)):
	ch=s[right]
	if ch in dic and dic[ch]>=left:
		left=dic[ch]+1
	dic[ch]=right
	res=max(res,right-left+1)
print(res)
```

```
s=input().strip()
p=input().strip()
need={}
window={}
for ch in p:
	need[ch]=need.get(ch,0)+1
left=0
right=0
valid=0
res=[]
while right<len(s):
	ch=s[right]
	right+=1
	if ch in need:
		window[ch]=
```

```
matrix=[]
for line in sys.stdin:
	row=list(map(int,line.split()))
	matrix.append(row)

```

```
n=int(input())
def shunshizheng(matrix):
	k=len(matrix)
	new_matrix=
```

```
nums=list(map(int,input().split()))
k=int(input())
n=len(nums)
count=0
pre_sum=0
dic={0:1}
for num in nums:
	pre_sum+=num
	need=pre_sum-k
	if need in dic:
		count+=dic[need]
	dic[pre_sum]=dic.get(pre_sum,0)+1
print(count)
```

对于哈希表老是一些用法记不住：
```
dic = {}

dic["apple"] = 3
dic["banana"] = 5

print(dic["apple"])   # 3
print(dic["banana"])  # 5
```
dic[[2]=0]
对应的是{2：0}

判断有没有出现
```
dic = {}

for i in range(len(nums)):
    x = nums[i]

    if x in dic:
        # 以前出现过
        pass
    else:
        # 第一次出现
        dic[x] = i
```

判断出现了几次：
```
s = "abbccc"
dic = {}

for ch in s:
    dic[ch] = dic.get(ch, 0) + 1

print(dic)
```
dic.get(ch, 0)的意思是，ch在字典里就取原来的值，不在就当做0
等价于
```
if ch in dic:
    dic[ch] += 1
else:
    dic[ch] = 1
```
两数之和：
key = 数字
value = 下标

字母异位词分组：
key = 排序后的字符串
value = 这一组单词列表

字符计数：
key = 字符
value = 出现次数

和为 K 的子数组：
key = 前缀和
value = 这个前缀和出现次数

和为K的子数组解法：
首先定义前n项的和，我们只关注和是多少，然后存储前缀和+他们的数量
一旦遇到了差need的前缀和，寻找字典里need的数量，有多少加多少
最后更新前缀和的统计，要注意初始情况是前缀和为0的有1个，就是一开始。

```
n=int(input())
nums=list(map(int,input().split()))
k=int(input())
q=deque()
res=[]
for i in range(n):
	while q and q[0]<=i-k:
		q.popleft()
	while q and nums[q[-1]]<=nums[i]:
		q.pop()
	q.append(i)
	if i>=k-1:
		res.append(nums[q[0]])
print(*res)

```
使用队列存储下标，第一个while循环，表示滑动窗口，下标小的就划出去了，第二个while循环表示的是把不可能成为答案的删掉，只要最大的数
不要混淆的是两个部分，第一个部分是窗口，第二个部分是单调队列
队列：
滑动窗口
从左边删除
从右边加入
```
s = input().strip()  
t = input().strip()
need={}
window={}
for ch in t:
	need[ch]=need.get(ch,0)+1
left=0
right=0
valid=0
start=0
min_len=float('inf')
while right<len(s):
	ch=s[right]
	right+=1
	if ch in need:
		window[ch]=window.grt(ch,0)+1
		if window[ch]==need[ch]:
			valid+=1
	while valid==len(need):
		if right-left<min_len:
			min_len=right=left
			start=left
		d=s[left]
		left+=1
		if d in need:
			if window[d]==need[d]:
				valid-=1
			window[d]-=1
if min_len==float('inf'):
	print("")
else:
	print(s[start:start+min_len])
```
要注意的是定义了什么，left和right是得到窗口的指针，valid是表示解决的的字母数量，字典是收集字母出现次数是否满足，当valid和字母个数相同时代表满足条件了，接下来就是收缩左边的指针，然后继续向右找

```
n = int(input())  
nums = list(map(int, input().split()))
cur=nums[0]
res=nums[0]
for i in range(1,n):
	cur=max(nums[i],cur+nums[i])
	res=max(res,cur)
print(res)
```

```
n=int(input())
intervals=[]
for _ in range(n):
	a,b=map(int,input().split())
	intervals.append([a,b])
intervals.sort()
res=[]
for interval in intervals:
	start=interval[0]
	end=interval[1]
	if not res or start>res[-1][1]:
		res.append([start,end])
	else:
		res[-1][1]=max(res[-1][1],end)
for interval in intervals:
	print(interval[0],interval[1])
```
排序后会按照起点从小到大排列
```
n = int(input())  
nums = list(map(int, input().split()))  
k = int(input())
k=k%n
nums=nums[-k:]+nums[:-k]
print(*nums)
```

```
n = int(input())  
nums = list(map(int, input().split()))
res=[1]*n
left_product=1
for i in range(n):
	res[i]=left_product
	left_product*=nums[i]

```
不能用除法的原因：数组里有0的时候会出错

```
m,n=map(int,input())
matrix=[]
for _ in range(m):
	row=list(map(int,input().split()))
	matrix.append(row)
top=0
bottom=m-1
left=0
right=n-1
res=[]
while top<=bottom and left<=right:
	for j in range(left,right+1):
		res.append(matrix[top][j])
	top+=1
	for j in range(top,bottom+1):
		res.append(matrix[j][right])
	right-=1
	if top<=bottom:
		for j in range(right,left-1,-1):
			res.append
	if left<=right:
		
		
```
螺旋矩阵：核心是维护边界，首先显而易见的大前提，然后第一次做没注意的是最后的约束情况，顺时针逆时针，可以动手画图
旋转矩阵，也就是要让m×n矩阵里原先在i,j这个位置的元素到新的n×m矩阵的第n+1-i，j这个位置，这个就是一个先交换ij，每一行倒过来，要注意的是循环次数，不要变成了循环两遍。
```
m,n=map(int,input().split())
matrix=[]
for _ in range(m):
	matrix.append(list(int,input().split()))
target=int(input())
i=0
j=n-1
found=False
while i<m and j>0:
	if matrix[i][j]==target:
		found=True
		break
	elif:matrix[i][j]>target:
		i-=1
	else:
		j+=1
print(found)
```

选择跳过链表和二叉树
```
m, n = map(int, input().split())  
  
grid = []  
for _ in range(m):  
	grid.append(input().split())
def dfs(i,j):
	if i<0 or i>=m or j<0 or j>=n:
		return 
	if grid[i][j]!='1':
		return
	grid[i][j]='0'
	
	dfs(i + 1, j)  
	dfs(i - 1, j)  
	dfs(i, j + 1)  
	dfs(i, j - 1)
count=0
for i in range(m):
	for j in range(n):
		if grid[i][j]=='1':
			count+=1
			dfs(i,j)
print(count)
		

```
学会使用dfs，深度优先搜索：什么叫DFS呢，DFS容易出现爆札的情况，迭代就是，如果这个满足，那就往下找，就很像一个树，顺序是从上到下先来。
BFS是广度优先搜索：避免递归太深，什么意思呢，就是我一层一层的往下看：
实现代码如下：
```
from collections import deque  
  
m, n = map(int, input().split())  
  
grid = []  
for _ in range(m):  
	grid.append(input().split())  
  
directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
count=0
for i in range(m):
	for j in range(n):
		if grid[i][j]=='1':
			count+=1
			grid[i][j]='0'
			q=deque()
			q.append((i,j))
			while q:
				x,y=q.popleft()
				for dx,dy in directions:
					nx=x+dx
					ny=y+dy
					if 0 <= nx < m and 0 <= ny < n and grid[nx][ny] == '1':
						grid[nx][ny]='0'
						q.append((nx,ny))
print(count)
```
这里就引入了队列的应用：用队列保存“接下来要处理的位置”。
q.append(x)      # 从右边加入x
q.popleft()      # 从左边取出
```

from collections import deque

q = deque()

q.append(1)
q.append(2)
q.append(3)

print(q.popleft())  # 1
print(q.popleft())  # 2
print(q.popleft())  # 3
```

```
from collections import deque

q = deque()
q.append((起点x, 起点y))

while q:
    x, y = q.popleft()

    for dx, dy in directions:
        nx = x + dx
        ny = y + dy

        if 新位置合法:
            标记新位置已经访问
            q.append((nx, ny))
```

x, y = q.popleft()意思是：从队列左边取出一个元素，并且把这个元素拆成 `x` 和 `y`。叫做解包赋值

```
import sys
from collections import deque

lines=sys.stdin.read().strip().splitlines()
grid=[]
for line in lines:
	grid.append(list(map(int,line.split())))
m=len(grid)
n=len(grid[0])
q=deque()
fresh=0
for i in range(m):
	for j in range(n):
		if grid[i][j]==2:
			q.append((i,j))
		elif grid[i][j]==1:
			fresh+=1
directions=[(1,0),(-1,0),(0,1),(0,-1)]
minutes=0
while q and fresh >0:
	size=len(q)
	for _ in range(size):
		x,y=q.popleft()
		for dx,dy in directions:
			nx=x+dx
			ny=y+dy
			if 0<=nx<m and 0<=ny<n and grid[nx][ny]==1:
				grid[nx][ny]=2
				fresh-=1
				q.append((nx,ny))
	minutes+=1
if fresh==0:
	print(minutes)
else:
	print(-1)
```

学习一下EOF的矩阵输入输出：
每行有空格：
```
lines=sys.stdin.read().strip().splitlines()
grid=[]
for line in lines:
	grid.append(list(map(int,line.split())))
	
```
每行没空格，是连续的字符串
```
lines=sys.stdin.read().strip().splitlines()
grid=[]
for line in lines:
	grid.append(list(line.strip()))
```

根据往年题，模拟类型的题目是必考的：其中矩阵处理：
首先是矩阵的输入输出处理，输入n×m的，EOF的都要掌握，输出
其次是矩阵的分块处理，这个leetcodehot100没有，定义一个分块函数
def fenkuai（matrix，r，c，size）：
然后是注意边界条件


现在是5月10日下午5点，进入回溯部分：
```
n = int(input())  
nums = list(map(int, input().split()))
res=[]
path=[]
used=[False]*n
def backtrack():
	if len(path)==n:
		res.append(path[:])
		return
	for i in range(n):
		if used[i]:
			continue
		path.append(nums[i])
		used[i]=True
		backtrack()
		used[i]=False
		path.pop()
backtrack()
for arr in res:
	print(*arr)
```
path.append(nums[i])
used[i] = True
把numsi加入队列，标记使用过
backtrack()
继续下一个位置
used[i] = False
path.pop()


```
n=int(input())
nums=list(map(int,input().split()))
res=[]
path=[]
def backtrack(start):
	res.append(path[:])
	for i in range(start,n):
		path.append(nums[i])
		backtrack(i+1)
		path.pop()
backtrack(0)
for arr in res:
	print(*arr)
```
想到回溯模板上加条件的变式
```
n=int(input())
nums=list(map(int,input().split()))
res=[]
path=[]
def traceback(start):
	res.append(path[:])
	for i in range(start,n):
		if nums[i]%2!=0:
			continue
		path.append(nums[i])
		backtrack(i+1)
		path.pop()
backtrack(0)
	
```
注意如果涉及子集和，要多传一个参数total回来
continue的意思是跳出此次循环
return是写在函数里，代表结束函数返回
回溯里面调用自身，感觉不好理解，或许以简单的例子来辅助理解？

```
digits=input().strip()
mp={
	'2': 'abc',  
	'3': 'def',  
	'4': 'ghi',  
	'5': 'jkl',  
	'6': 'mno',  
	'7': 'pqrs',  
	'8': 'tuv',  
	'9': 'wxyz'
}
res=[]
path=[]
def backtrack(index):
	if index==len(digits):
		res.append(''.join(path))
		return
	num=digits[index]
	letter=mp[num]
	for ch in letters:
		path.append(ch)
		backtrack(index+1)
		path.pop()
if digits:
	backtrack(0)
print(*res)
```

```
candidates=list(map(int,input().split()))
n=len(candidates)
target=int(input())
res=[]
path=[]
total=0
def backtrack(start,total):
	if total==target:
		res.append(path[:])
		return
	if total>target:
		return
	for i in range(start,n):
		path.append(candidate[i])
		
		backtrack(i,total+candidates[i])
		path.pop()
backtrack(0,0)
for arr in res:
	print(*arr)

```
要注意的判别条件，而且注意是组合不是排列，所有要返回i，而不是从0开始，也不是i+1,最后处理输出的格式是for arr in res:    print(*arr)

```
n=int(input())

path=[]
res=[]
def backtrack(left,right):
	if len(path)==2*n:
		res.append(''.join(path))
		return
	if left<n:
		path.append('(')
		backtrack(left + 1, right)  
		path.pop()  
  
	if right < left:  
		path.append(')')  
		backtrack(left, right + 1)  
		path.pop()  
  
backtrack(0, 0)  
  
for s in res:  
	print(s)
```
一般情况下输出要输出字符串，所以用了res.append(''.join(path))，使用print默认换行。nums = [1, 2, 3]

for x in nums:
    print(x, end=" ")
或者写成```
```
res = ["((()))", "(()())", "(())()"]
print(*res)

print(*res, sep="\n")#表示换行

```

单词搜索：
```
import sys
lines=sys.stdin.read().strip().splitlines()
word=lines[-1].strip()
board=[]
for line in lines[:-1]:
	if " " in line:#处理有空格隔开的输入格式
		board.append(line.split())
	else:处理连着输入字符串的
		board.append(list(line.split()))
m=len(board)
n=len(board[0])
def dfs(i,j,k):
	if k==len(word):
		return True
	if i<0 or i>=m or j<0 or j>=n:
		return False
	if board[i][j]!=word[k]:
		return False
	temp=board[i][j]
	board[i][j]='#'
	found = (  
		dfs(i + 1, j, k + 1) or  
		dfs(i - 1, j, k + 1) or  
		dfs(i, j + 1, k + 1) or  
		dfs(i, j - 1, k + 1)  
	)
	bound[i][j]=temp
	return found
ans=False
for i in range(m):
	for j in range(n):
		if dfs(i,j,0):
			ans=True
			break
	if ans:
		break
print(ans)
```
递归这种要注意细节，已经用过的格子不能再走回来，最后判断完要恢复，因为要开始试别的路径

一旦出现最短等字样：优先使用BFS
```
from collections import deque
m,n=map(int,input().split())
board=[]
for _ in range(m):
	board.append(input().split())
target=input().strip()
need={}
for ch in target:
	need[ch]=need.get(ch,0)+1
chars=list(need.keys())


```
enumerate(chars):同时拿到下标和值

```
s=input().strip()
n=len(s)
res=[]
path=[]
def ispa(word):
	return word==word[::-1]
def backtrack(start):
	if start==n:
		res.append(path[:])
		return
	for end in range(start,n):
		part=s[start,end+1]
		if not ispa(part):
			continue
		path.append(part)
		backtrack(end+1)
		path.pop()
backtrack(0)
for arr in res:
	print(*arr)
```
分割回文串没什么好说的，记住列表倒过来的写法就行word[::-1]

```
n=int(input())
res=[]


```
N皇后，这里先引入一下集合SET的用法:
```
nums = [1, 2, 2, 3, 3, 3]

s = set(nums)

print(s)#输出的就是{1，2，3}
#创建空集合
s=set()#不能写成s={}

s = {1, 2, 3}

print(2 in s)#TRUE
print(5 in s)#FALSE

s.remove(2)  s.discard(5)  s.add(3)
```

```
nums=list(map(int,input().split()))
n=len(nums)
target=int(input())
left=0
right=n-1
ans=-1
while left<=right:
	mid=(left+right)//2
	if nums[mid]==target:
		ans=mid
		break
	if nums[left]<nums[mid]:
		if nums[left]<=target<nums[mid]:
			right=mid-1
		else:
			left=mid+1
	else:
		if nums[mid]<target<=nums[right]:
			left=mid+1
		else:
			right=mid-1
print(ans)
			
```

```
nums=list(map(int,input().split()))
n=len(nums)
left=0
right=n-1
while left<right:
	mid=(left+right)//2
	if nums[mid]>nums[right]:
		left=mid+1
	else:
		right=mid
print(nums[left])
```

```

```